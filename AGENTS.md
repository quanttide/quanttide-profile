# AGENTS.md — Agent 工作指南

本文档为 Agent（包括 CodeBuddy、Claude Code、Cursor、GitHub Copilot 等）在本仓库中工作提供指南。

## 项目定位

量潮工作档案（quanttide-profile）是量潮知识管理体系中的**档案聚合容器**——按领域聚合各业务的工作档案子仓库（git submodule）。在"正交分解"中对应**主体轴（Who it is）**：`default/company` 为法人主体档案，`domains/` 下每个子目录为一个领域档案仓库，回答"我们是谁、各领域如何运作"。

## 工作原则

1. **最小干预**：仅在用户明确请求时操作
2. **分层提交**：子模块先提交推送 → 父仓库更新引用指针 → 提交推送
3. **禁止越级**：不在子仓库里做父仓库的事（如改父仓库的 README/CHANGELOG），也不要反过来
4. **减法优先**：删除无效内容优先于新增，宁可空着不放不确定的东西
5. **提交即推送**：提交后默认推送远端（主仓库和子模块都推），除非用户明确说"只提交不推"
6. **维护记录**：修改后同步更新 CHANGELOG.md

## 子模块管理

- 子模块独立维护，父仓库只追踪引用
- **禁止**：直接在父仓库修改子模块文件（`default/company`、`domains/*` 下的文件属于各自子仓库）
- **必须**：在子模块仓库独立提交推送，父仓库只更新引用
- 新增档案仓库：`git submodule add <url> domains/<path>`
- 同步全部子模块：`git submodule update --init --recursive`

## 文档规范

- 中文、Markdown
- 本仓库自身文档（README/AGENTS/CHANGELOG）回答"容器如何组织"，各领域内容见对应子模块
- 各子模块有自己的 README/AGENTS，开发前查阅对应模块

## AI 执行指引

过往经验表明 AI 最容易在这些地方犯错，请特别注意：

- **子仓库 vs 主仓库**：不要在子仓库里做主仓库的事（如改其 CHANGELOG），也不要反过来——先确认当前工作对象属于哪个仓库
- **替换 vs 并存**：修改内容前先确认旧内容怎么处理，不要默认替换
- **改内容 vs 改名字**：操作前明确目标是文件/目录的内容还是名称
- **目录即语义**：目录名使用代码实际结构，不用抽象概念。有实体才建目录，但预留目录也是有效的架构声明——不要擅自移除用户放置的空目录和占位文件
- **指针更新**：子模块版本变化后，父仓库提交时确认 `.gitmodules` 与目录结构一致（README 中的结构图同步更新）

### 特殊文档提醒

完成重要变更后，检查是否需要同步更新以下文档：

| 文档 | 触发条件 |
|------|---------|
| [CHANGELOG.md](CHANGELOG.md) | 对用户可见的变更（新功能、重构、修复） |
| [README.md](README.md) | 仓库结构或关联关系变化（如新增/移除子模块） |
| [AGENTS.md](AGENTS.md) | AI 工作经验增加 |

AI 应在完成工作后主动提醒用户是否需要更新这些文档，而非等用户提出。
